// Owl-Carousel Start.

$(document).ready(function () {
	// I am commenting this thing out to fix the issue
    // $("#owl-demo").owlCarousel({
        
    // });

    //Now the function will be called again. This time it will take an object parameter with three properties with respective values.After that the code will work accordingly.
    $('.owl-carousel').owlCarousel({
        loop:true,
        margin:10,
        nav:true
    })
});
// Owl-Carousel End.

